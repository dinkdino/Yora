<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rush.yora">

    <application
        android:name=".core.YoraApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".base.BaseActivity" />
        <activity android:name=".base.BaseAuthenticatedActivity" />
        <activity android:name=".auth.login.LoginActivity" />
        <activity android:name=".home.HomeActivity"></activity>
    </application>

</manifest>